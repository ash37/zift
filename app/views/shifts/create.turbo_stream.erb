<%= turbo_stream.append "roster_day_#{@shift.user_id}_#{@shift.start_time.to_date}" do %>
  <%= render "shifts/shift", shift: @shift %>
<% end %>


<%= turbo_stream.replace "assign_shift_modal", "" %>
