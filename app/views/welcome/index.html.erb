<div class="text-center py-24">
  <h1 class="text-4xl font-bold tracking-tight text-gray-900 sm:text-6xl">
    Welcome to Qcare
  </h1>
  <p class="mt-6 text-lg leading-8 text-gray-600">
    Your shift scheduling, simplified.
  </p>
  <div class="mt-10 flex items-center justify-center gap-x-6">
    <% if user_signed_in? %>
      <%= link_to "Go to Dashboard", dashboards_path, class: "rounded-md bg-violet-600 px-3.5 py-2.5 text-sm font-semibold text-white shadow-sm hover:bg-violet-500 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-violet-600" %>
    <% else %>
      <%= link_to "Get started", new_application_path, class: "rounded-md bg-violet-600 px-3.5 py-2.5 text-sm font-semibold text-white shadow-sm hover:bg-violet-500 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-violet-600" %>
      <%= link_to "Log in <span aria-hidden='true'>→</span>".html_safe, new_user_session_path, class: "text-sm font-semibold leading-6 text-gray-900" %>
    <% end %>
  </div>
</div>