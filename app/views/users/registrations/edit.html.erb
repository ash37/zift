<%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put, class: "space-y-6" }) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>
      <%= f.hidden_field :invitation_token, value: params[:invitation_token] || resource.invitation_token %>
      <% user = @user || resource %>

<div class="bg-zinc-100 pt-4 sm:py-12 px-0 sm:px-6 lg:px-8">
  <div class="mt-6 w-full rounded-none bg-zinc-100 p-8 sm:mt-10 sm:max-w-4xl sm:mx-auto sm:rounded-3xl">
    <div class="text-center mb-8">
      <span class="text-4xl font-bold text-gray-800">Welcome to the</span><span class="text-4xl font-bold text-violet-700"> team</span>
      <p class="mt-2 text-gray-600">Please set your password and complete your employee file to get started.</p>
    </div>
  </div>
</div>

    
<div class="bg-zinc-100 min-h-screen">
  <section class="container mx-auto px-4 py-10">
    <div class="w-full max-w-4xl mx-auto">

      <div class="bg-white rounded-2xl p-6 flex items-center justify-between">
        <div class="flex items-center min-w-0">
          <div class="text-left">
            <h1 class="text-2xl font-bold text-gray-800 truncate"><%= user.name %></h1>
            <p class="text-gray-600 break-all sm:break-normal"><%= user.email %></p>
            <p class="text-gray-600 break-all sm:break-normal"><%= user.phone %></p>
          </div>
        </div>
        <% dob_display = user.date_of_birth.present? ? user.date_of_birth.strftime('%-d %b %Y').upcase : '—' %>
        
         <!-- Date of birth -->
    <div class="flex items-start gap-3 p-3 rounded-2xl hover:bg-gray-50">
      <!-- Heroicon: Calendar -->
          <div class="flex-shrink-0 w-10 h-10 flex items-center justify-center bg-violet-100 rounded-full">
            <svg class="h-5 w-5 text-violet-600" viewBox="0 0 24 24" fill="none" aria-hidden="true">
              <path d="M7 3v3M17 3v3M3 11h18" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
              <rect x="3" y="6" width="18" height="15" rx="2" stroke="currentColor" stroke-width="2"/>
            </svg>
          </div>
      
      <div class="min-w-0">
        <div class="text-xs uppercase tracking-wide text-gray-500">Date of birth</div>
        <div class="text-gray-800"><%= dob_display %></div>
      </div>
    </div>
      </div>
 <div class="hidden sm:block">
    <%= render "users/registrations/desktop_registration", f: f %>
  </div>

  <div class="sm:hidden">
    <%= render "users/registrations/mobile_registration", f: f %>
  </div>
    
   <% end %>