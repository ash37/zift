<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>New Unavailability Request</title>
  <style>
    body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif; background:#f9fafb; margin:0; padding:24px; color:#1f2937; }
    .card { max-width: 640px; margin:0 auto; background:#fff; border-radius:16px; padding:28px 32px; box-shadow:0 10px 30px rgba(15, 23, 42, 0.08); }
    h1 { font-size: 22px; margin:0 0 16px; color:#4c1d95; }
    h2 { font-size:16px; margin:24px 0 12px; color:#4c1d95; text-transform:uppercase; letter-spacing:0.08em; }
    .row { display:flex; gap:18px; margin-bottom:14px; }
    .col { flex:1; }
    .label { font-size:11px; text-transform:uppercase; letter-spacing:0.1em; color:#6b7280; margin-bottom:4px; }
    .value { font-size:15px; font-weight:600; color:#111827; }
    .box { background:#f3f4f6; border-radius:12px; padding:14px; margin-top:10px; }
    .muted { color:#4b5563; font-size:14px; line-height:1.5; white-space:pre-wrap; }
  </style>
</head>
<body>
  <div class="card">
    <h1>New unavailability request submitted</h1>
    <p class="muted" style="margin-top:4px;">A team member has submitted time off. Details are below.</p>

    <h2>Employee</h2>
    <div class="row">
      <div class="col">
        <div class="label">Name</div>
        <div class="value"><%= @user.name %></div>
      </div>
      <div class="col">
        <div class="label">Email</div>
        <div class="value"><%= @user.email %></div>
      </div>
    </div>

    <h2>Requested period</h2>
    <div class="row">
      <div class="col">
        <div class="label">Starts</div>
        <div class="value"><%= @request.starts_at&.strftime('%A, %d %B %Y %I:%M %p') if @request.starts_at %></div>
      </div>
      <div class="col">
        <div class="label">Ends</div>
        <div class="value"><%= @request.ends_at&.strftime('%A, %d %B %Y %I:%M %p') if @request.ends_at %></div>
      </div>
      <div class="col">
        <div class="label">All day?</div>
        <div class="value"><%= @request.all_day? ? 'Yes' : 'No' %></div>
      </div>
    </div>

    <% if @request.repeats_weekly? %>
      <div class="box">
        <div class="label">Repeats weekly</div>
        <p class="muted">This request repeats every <strong><%= @request.starts_at.strftime('%A') if @request.starts_at %></strong>.</p>
      </div>
    <% end %>

    <div class="box">
      <div class="label">Reason</div>
      <p class="muted"><%= @request.reason %></p>
    </div>

    <div class="row" style="margin-top:18px;">
      <div class="col">
        <div class="label">Current status</div>
        <div class="value"><%= @request.status_name %></div>
      </div>
      <div class="col">
        <div class="label">Submitted</div>
        <div class="value"><%= @request.created_at.strftime('%A, %d %B %Y %I:%M %p') %></div>
      </div>
    </div>
  </div>
</body>
</html>
